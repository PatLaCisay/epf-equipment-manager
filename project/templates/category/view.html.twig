{% extends 'base.html.twig' %}

{% block title %}Détail de la categorie {{ category.name }}{% endblock %}

{% block body %}

<h1>Détail de la categorie {{ category.name }}</h1>
<div class="container">
    <table class="table table-striped">
        <thead>
            <tr>
                <th>Nom</th>
                <th>Id</th>
                <th>Description</th>
            </tr>
        </thead>
        <tbody>
            <tr>
                <td>{{ category.name }}</td>
                <td>{{ category.id }}</td>
                <td>{{ category.description }}</td>
            </tr>
        </tbody>
    </table>
</div>

{% endblock %}
